import { EventsScreen } from "@/screens";

export const dynamic = "force-dynamic";
export const metadata = { title: "All Events — Utsav Events" };

export default function Page() {
  return <EventsScreen />;
}
