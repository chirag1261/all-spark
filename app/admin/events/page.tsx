import { AdminEventsScreen } from "@/screens";

export const dynamic = "force-dynamic";
export const metadata = { title: "Events — Utsav Events Admin" };

export default function Page() {
  return <AdminEventsScreen />;
}
