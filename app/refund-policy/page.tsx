import { RefundPolicyScreen } from "@/screens";

export const metadata = { title: "Refund & Cancellation Policy — Utsav Events" };

export default function Page() {
  return <RefundPolicyScreen />;
}
