import { AdminEventCreateScreen } from "@/screens";

export const dynamic = "force-dynamic";
export const metadata = { title: "Create event — Utsav Events Admin" };

export default function Page() {
  return <AdminEventCreateScreen />;
}
