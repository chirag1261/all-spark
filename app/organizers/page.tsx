import { OrganizersScreen } from "@/screens";

export const metadata = { title: "Organizers — Utsav Events" };
export const dynamic = "force-dynamic";

export default function Page() {
  return <OrganizersScreen />;
}
